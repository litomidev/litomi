export enum BookmarkSource {
  HASHA = 0,
  HARPI = 1,
  HIYOBI = 2,
  K_HENTAI = 3,
  // E_HENTAI = 4,
  // EX_HENTAI = 5,
}

export enum CensorshipKey {
  TAG = 0,
  ARTIST = 1,
  CHARACTER = 2,
  GROUP = 3,
  SERIES = 4,
}

export enum CensorshipLevel {
  LIGHT = 0,
  HEAVY = 1,
}
